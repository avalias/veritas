# Cargo.lock and Move.lock are deliberately committed: pinned dependencies
# are part of the determinism story (golden trace hashes must reproduce
# from a fresh clone).
/target
dispute/build/
# 1.5 GB weights stay out of git — pinned by hash in models/qwen/artifacts/
models/qwen/artifacts/model.safetensors
.DS_Store
