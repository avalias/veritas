# Cargo.lock and Move.lock are deliberately committed: pinned dependencies
# are part of the determinism story (golden trace hashes must reproduce
# from a fresh clone).
/target
**/build/
**/node_modules/
# 1.5 GB weights stay out of git, pinned by hash in opml/models/qwen/artifacts/
opml/models/qwen/artifacts/model.safetensors
# self-hosted zkTLS attestor: a third-party clone plus its downloaded zk files
tools/zktls/attestor-core/
.DS_Store
