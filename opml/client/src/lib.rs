//! Phase 2 client: `gen_move_vectors` emits the Rust↔Move equivalence
//! suite; `localnet_demo` drives a real on-chain dispute against `sui
//! start` (see bin sources for usage).

pub mod localnet;
